import { ObjectId } from "mongoose";

// ----------------------------- Company Interface

export interface companyInterface {
  _id: ObjectId;
  price: String;
  discount: String;
}

// ----------------------------- Company Interface
